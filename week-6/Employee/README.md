# Employee Application

This is the Employee management application developed during Week 6.

## Overview
This application serves as a frontend interface for managing employee data. It is built using modern React concepts.

## Setup
1. Navigate to this directory.
2. Run `npm install` to install dependencies.
3. Run `npm start` or `npm run dev` to start the application.
